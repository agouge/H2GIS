---
---
[
    {% for p in site.pages %}
    {
      "title"       : "{{ p.title }}",
      "href"        : "{{ p.url }}",
      "description" : "{{ p.description }}"
    }
    {% unless forloop.last %},{% endunless %}
    {% endfor %}
]
